<h1>Edit Employment Record</h1>

<%= render 'form' %>

<%= link_to 'Destroy', employment_record_path(@employment_record, user_id: @user_id), method: :delete, data: { confirm: 'Are you sure?' } %> |

<%= link_to 'Back', employment_records_path(user_id: @user_id) %>